<h3>Users</h3>

<% if @pets.empty? %>
	<h1>No posts made</h1>
<% else %>
	<div>
		<% @pets.each do |pet| %>
			<div>
				<ul>
					<li><%= link_to pet.name, user_path(pet)  %></li>
				</ul>
			</div>
		<% end %>
	</div>
<% end %>

